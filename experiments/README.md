# Experiments

Use this directory for exported MLflow artifacts, serialized checkpoints, and lightweight training manifests.

## Expected subfolders
- `manifests/`: dataset summaries emitted by the ML training flow.
- `runs/`: per-training JSON summaries including metrics and dataset references.
